#include <stdio.h>
/**
 * main - function to print it's name
 * @argc: arguments count
 * @argv: arguments value
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}

